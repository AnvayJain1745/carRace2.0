var backGroundImage 
function setup() {
  createCanvas(displayWidth, displayHeight);
}
function preload(){
  backGroundImage=loadImage("background Image.jpg")
}
function draw() {
  background(220);
 
}